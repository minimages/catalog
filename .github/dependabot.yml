version: 2
updates:
- package-ecosystem: "github-actions"
  directory: "/"
  schedule:
    interval: "weekly"
  labels: [deps, ci]
- package-ecosystem: "terraform"
  directory: "/"
  schedule:
    interval: "weekly"
  labels: [deps, terraform]
